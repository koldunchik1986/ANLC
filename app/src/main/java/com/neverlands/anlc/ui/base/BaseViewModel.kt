package com.neverlands.anlc.ui.base

import androidx.lifecycle.ViewModel

/**
 * Базовый ViewModel для всех ViewModel в приложении.
 * Здесь можно добавить общую логику, если потребуется.
 */
open class BaseViewModel : ViewModel() {
    // Пока пустой, но может быть расширен в будущем
}
